const nome = "Choi Jongho"; // String
const age = 23; // Number

let nomeAluno; // undefined = não aponta pra local nenhum na memoria
let sobrenomeAluno = null; // Nulo = não ter valor na memoria (lógico])

console.log(typeof nomeAluno) // retorna number

const aprovado = true; // boolean - true or false

const a = [1 , 2];
const b = a;

console.log(a,b)

b.push(3); //adicionou mais um
console.log(b)